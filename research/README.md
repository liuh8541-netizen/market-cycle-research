# 隔離研究區

這個目錄專供尚未通過驗證的實驗使用。

隔離規則：

- 研究程序可唯讀使用正式區的既有資料。
- 研究資料、快照、manifest、checkpoint、程式與報告只寫入 `research/`。
- 正式區的 `data/`、`reports/`、`config/`、`scripts/`、`src/` 與批次檔在研究期間不得修改。
- 研究失敗不影響 `run_predict_market.bat` 的日常預測。
- 實驗通過預先鎖定門檻，也不會自動更新正式程式；必須先提出證據並取得使用者明確同意。

